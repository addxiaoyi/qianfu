import React, { useMemo, useState } from 'react';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { api } from '@/api/request';
import StatusWrapper from '@/components/StatusWrapper';
import AdminPageHeader from '@/components/AdminPageHeader';
import AdminStatCard from '@/components/AdminStatCard';
import { toast } from '@/hooks/use-toast';
import { motion, AnimatePresence } from 'framer-motion';
import { AlertTriangle, Copy, CheckCircle2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import GeometricLantern from '@/components/icons/GeometricLantern';
import AdminPromoDraftView from './components/AdminPromoDraftView';
import AdminPromoTaskTable from './components/AdminPromoTaskTable';
import { promoQuickSetupTips } from './promoConfig';

const AdminPromoTasks: React.FC = () => {
  const queryClient = useQueryClient();
  const [activeTab, setActiveTab] = useState<'all' | 'enabled' | 'draft' | 'disabled' | 'paused'>('all');
  const [showCreateTips, setShowCreateTips] = useState(true);
  const [confirmAction, setConfirmAction] = useState<{ id: number; type: 'pause' | 'disable'; title: string; confirmText: string; input: string; countdown: number } | null>(null);
  const [selectedDraftIds, setSelectedDraftIds] = useState<number[]>([]);

  const { data, isLoading, isError, refetch } = useQuery({ queryKey: ['admin-promo-tasks'], queryFn: () => api.get<any[]>('/promo/tasks') });
  const tasks = (Array.isArray(data) ? data : ((data as any)?.tasks ?? [])) as any[];
  const filtered = useMemo(() => tasks.filter((task: any) => activeTab === 'all' || String(task.status || '').toLowerCase() === activeTab), [tasks, activeTab]);
  const draftTasks = useMemo(() => tasks.filter((task: any) => String(task.status || '').toLowerCase() === 'draft'), [tasks]);
  const selectedDraftTasks = useMemo(() => draftTasks.filter((task: any) => selectedDraftIds.includes(task.id)), [draftTasks, selectedDraftIds]);

  const createMutation = useMutation({ mutationFn: () => api.post('/promo/tasks', { title: 'B站三连奖励', description: '点赞、投币、收藏后自动发放余额奖励。', platform: 'bilibili', targetType: 'video', targetId: 'BV1demo000001', targetUrl: 'https://www.bilibili.com/video/BV1demo000001', coverUrl: 'https://picsum.photos/seed/promo-task/1200/800', rewardAmount: 5, rewardType: 'BALANCE', ruleConfig: { actions: { like: true, coin: true, favorite: true, follow: false, share: false }, condition: 'all_required' }, claimLimitPerUser: 1, needAudit: false, autoVerify: true, status: 'DRAFT' }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); queryClient.invalidateQueries({ queryKey: ['admin-promo-summary'] }); toast({ title: 'TASK_CREATED', description: 'Demo promo task has been created successfully.' }); }, onError: () => toast({ variant: 'destructive', title: 'CREATE_FAILED', description: 'Could not create promo task.' }) });
  const publishMutation = useMutation({ mutationFn: (id: number) => api.post(`/promo/tasks/${id}/publish`, {}), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'TASK_PUBLISHED', description: 'Task has been published.' }); } });
  const pauseMutation = useMutation({ mutationFn: ({ id }: { id: number; remark: string }) => api.post(`/promo/tasks/${id}/pause`, { remark: 'admin action' }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'TASK_PAUSED', description: 'Task has been paused.' }); } });
  const disableMutation = useMutation({ mutationFn: ({ id }: { id: number; remark: string }) => api.post(`/promo/tasks/${id}/disable`, { remark: 'admin action' }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'TASK_DISABLED', description: 'Task has been disabled.' }); } });
  const batchPublishMutation = useMutation({ mutationFn: (ids: number[]) => api.post('/promo/tasks/batch/publish', { ids }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'BATCH_PUBLISHED', description: 'Selected drafts have been published.' }); setSelectedDraftIds([]); } });
  const batchPauseMutation = useMutation({ mutationFn: (ids: number[]) => api.post('/promo/tasks/batch/pause', { ids }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'BATCH_PAUSED', description: 'Selected drafts have been paused.' }); setSelectedDraftIds([]); } });
  const batchRestoreMutation = useMutation({ mutationFn: (ids: number[]) => api.post('/promo/tasks/batch/restore', { ids }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['admin-promo-tasks'] }); toast({ title: 'BATCH_RESTORED', description: 'Selected drafts have been restored.' }); setSelectedDraftIds([]); } });

  const stats = [
    { label: '任务总数', value: String(tasks.length), variant: 'network' as const, color: 'text-green-500', trend: '已配置任务', tag: 'PT_01' },
    { label: '启用任务', value: String(tasks.filter((t: any) => String(t.status).toLowerCase() === 'enabled').length), variant: 'spark' as const, color: 'text-blue-500', trend: '可领取', tag: 'PT_02' },
    { label: '草稿任务', value: String(draftTasks.length), variant: 'settings' as const, color: 'text-zinc-400', trend: '待发布', tag: 'PT_03' },
    { label: '暂停任务', value: String(tasks.filter((t: any) => String(t.status).toLowerCase() === 'paused').length), variant: 'alert' as const, color: 'text-amber-500', trend: '临时停止', tag: 'PT_04' },
  ];

  const performAction = async () => { if (!confirmAction) return; if (confirmAction.countdown > 0 || confirmAction.input !== confirmAction.confirmText) return; const payload = { id: confirmAction.id, remark: confirmAction.input }; if (confirmAction.type === 'pause') await pauseMutation.mutateAsync(payload); if (confirmAction.type === 'disable') await disableMutation.mutateAsync(payload); setConfirmAction(null); };
  const openConfirm = (task: any, type: 'pause' | 'disable') => setConfirmAction({ id: task.id, type, title: task.title, confirmText: task.title, input: '', countdown: 2 });
  const copyDraftTitles = async () => navigator.clipboard.writeText(selectedDraftTasks.map((task) => task.title).join('\n'));
  const matchesConfirm = Boolean(confirmAction && confirmAction.input === confirmAction.confirmText && confirmAction.countdown === 0);
  React.useEffect(() => { if (!confirmAction || confirmAction.countdown <= 0) return; const timer = window.setTimeout(() => setConfirmAction((prev) => prev ? { ...prev, countdown: prev.countdown - 1 } : prev), 1000); return () => window.clearTimeout(timer); }, [confirmAction]);

  const visibleTaskTable = (<AdminPromoTaskTable tasks={filtered} onPublish={(id) => publishMutation.mutate(id)} onPause={(task) => openConfirm(task, 'pause')} onDisable={(task) => openConfirm(task, 'disable')} />);

  return (
    <div className="space-y-16 pb-32 bg-white selection:bg-accent selection:text-white">
      <StatusWrapper isLoading={isLoading} isError={isError} onRetry={() => refetch()}>
        <AdminPageHeader badge="PROMO_TASKS / REWARD_AUTHORING" title="任务列表" description="管理每个推广任务的规则、奖励、领取限制和发放方式。你可以在这里快速创建一个默认的 B 站三连任务。" statusLabel="Task Registry: LIVE" rightSlot={<button onClick={() => createMutation.mutate()} className="group px-12 py-8 btn-accent rounded-[3rem] text-[12px] font-black uppercase tracking-[0.5em] transition-all flex items-center gap-6 shadow-2xl shadow-accent/20 italic active:scale-[0.98]"><GeometricLantern variant="spark" className="w-6 h-6 group-hover:rotate-12 transition-transform duration-500" /> CREATE_SAMPLE_TASK</button>} />
        <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-10">{stats.map((s, idx) => <AdminStatCard key={s.tag} tag={s.tag} value={s.value} label={s.label} variant={s.variant} colorClassName={s.color} trend={s.trend} delay={idx * 0.08} />)}</div>
        <div className="flex flex-col xl:flex-row items-center justify-between gap-10 pt-12 border-t border-zinc-50"><div className="flex gap-16 overflow-x-auto no-scrollbar w-full xl:w-auto">{[{ id: 'all', label: 'All Tasks', tag: 'ALL' }, { id: 'enabled', label: 'Enabled', tag: 'ON' }, { id: 'draft', label: 'Drafts', tag: 'DRAFT' }, { id: 'paused', label: 'Paused', tag: 'HOLD' }, { id: 'disabled', label: 'Disabled', tag: 'OFF' }].map((tab) => <button key={tab.id} onClick={() => setActiveTab(tab.id as any)} className={`flex flex-col items-start gap-3 pb-10 transition-all relative group ${activeTab === tab.id ? 'opacity-100' : 'opacity-40 hover:opacity-100'}`}><span className="text-[12px] font-black uppercase tracking-[0.4em] italic">{tab.label}</span><span className="text-[9px] font-black font-mono tracking-widest text-zinc-400">/ {tab.tag}</span>{activeTab === tab.id && <motion.div layoutId="promo-task-tab" className="absolute bottom-0 left-0 right-0 h-1.5 bg-accent" />}</button>)}</div><div className="flex gap-4"><Link to="/admin-promo/create" className="px-10 py-6 bg-zinc-50/50 border border-transparent hover:border-accent hover:bg-white rounded-[2rem] text-[10px] font-black uppercase tracking-[0.4em] flex items-center gap-4 transition-all duration-500 italic shadow-xs group"><GeometricLantern variant="settings" className="w-4 h-4 text-zinc-200 group-hover:text-accent transition-colors" /> Create Page</Link><button onClick={() => setShowCreateTips((v) => !v)} className="px-10 py-6 bg-zinc-50/50 border border-transparent hover:border-accent hover:bg-white rounded-[2rem] text-[10px] font-black uppercase tracking-[0.4em] flex items-center gap-4 transition-all duration-500 italic shadow-xs group"><GeometricLantern variant="settings" className="w-4 h-4 text-zinc-200 group-hover:text-accent transition-colors" /> Toggle Tips</button></div></div>
        <AnimatePresence>{showCreateTips && (<motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 10 }} className="p-12 border border-accent/10 rounded-[4rem] bg-accent/5 space-y-6"><div className="flex items-center gap-4"><GeometricLantern variant="activity" className="w-6 h-6 text-accent" /><div className="text-[11px] font-black uppercase tracking-[0.4em] italic text-accent">Quick Setup</div></div><div className="grid grid-cols-1 lg:grid-cols-3 gap-6 text-[11px] font-medium text-zinc-500 leading-7">{promoQuickSetupTips.map((tip) => <div key={tip} className="p-8 bg-white rounded-[2rem] border border-zinc-100">{tip}</div>)}</div></motion.div>)}</AnimatePresence>
        {activeTab === 'draft' ? <AdminPromoDraftView draftTasks={draftTasks} selectedDraftIds={selectedDraftIds} setSelectedDraftIds={setSelectedDraftIds} onBatchPublish={() => batchPublishMutation.mutate(selectedDraftIds)} onBatchPause={() => batchPauseMutation.mutate(selectedDraftIds)} onCopyTitles={copyDraftTitles} selectedCount={selectedDraftIds.length} onBatchRestore={() => batchRestoreMutation.mutate(selectedDraftIds)} onPublishNow={(id) => publishMutation.mutate(id)} onPause={(task) => openConfirm(task, 'pause')} /> : visibleTaskTable}
      </StatusWrapper>

      <AnimatePresence>{confirmAction && (<div className="fixed inset-0 z-[1000] flex items-center justify-center p-8"><motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="absolute inset-0 bg-black/80 backdrop-blur-xl" onClick={() => setConfirmAction(null)} /><motion.div initial={{ opacity: 0, y: 16, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 16, scale: 0.98 }} className={`relative w-full max-w-xl bg-white rounded-[3rem] p-10 shadow-[0_40px_120px_rgba(0,0,0,0.45)] space-y-6 ring-1 ${confirmAction.type === 'pause' ? 'ring-amber-200' : 'ring-rose-200'}`}><div className="flex items-start gap-4"><div className={`w-12 h-12 rounded-2xl flex items-center justify-center ${confirmAction.type === 'pause' ? 'bg-amber-100 text-amber-700' : 'bg-rose-100 text-rose-700'}`}><AlertTriangle className="w-6 h-6" /></div><div><div className={`text-[10px] font-black uppercase tracking-[0.5em] italic ${confirmAction.type === 'pause' ? 'text-amber-600' : 'text-rose-600'}`}>{confirmAction.type === 'pause' ? 'Warning Zone' : 'Danger Zone'}</div><h3 className="text-2xl font-black uppercase italic tracking-tighter mt-2">{confirmAction.type === 'pause' ? 'Pause task' : 'Disable task'}</h3><p className="mt-3 text-sm text-zinc-500 leading-7">请输入任务名 <span className="font-black text-zinc-900">{confirmAction.confirmText}</span> 以确认执行 <span className="font-black">{confirmAction.type === 'pause' ? '暂停' : '下线'}</span> 操作。</p></div></div><div className="flex items-center gap-3"><input value={confirmAction.input} onChange={(e) => setConfirmAction((prev) => prev ? { ...prev, input: e.target.value } : prev)} placeholder={`Type "${confirmAction.confirmText}"`} className="flex-1 px-5 py-4 rounded-[1.25rem] border border-zinc-200 bg-zinc-50 outline-none focus:border-accent font-mono text-sm" /><button onClick={() => navigator.clipboard.writeText(confirmAction.confirmText)} className="px-4 py-4 rounded-[1.25rem] border border-zinc-200 bg-white text-[10px] font-black uppercase tracking-[0.3em] italic flex items-center gap-2"><Copy className="w-4 h-4" /> Copy</button></div><div className="flex items-center gap-3 text-[10px] font-black uppercase tracking-[0.35em] italic text-zinc-400"><CheckCircle2 className={`w-4 h-4 ${matchesConfirm ? 'text-emerald-500' : 'text-zinc-300'}`} /> {confirmAction.countdown > 0 ? `Confirm in ${confirmAction.countdown}s` : matchesConfirm ? 'Name matched, confirm enabled.' : 'Confirm button will enable after exact match.'}</div><div className="p-4 rounded-[1.5rem] bg-zinc-50 border border-zinc-100 text-[10px] font-black uppercase tracking-[0.35em] italic text-zinc-400 leading-7">Paused tasks remain recoverable. Disabled tasks are intended for final shutdown or cleanup.</div><div className="flex justify-end gap-3"><button onClick={() => setConfirmAction(null)} className="px-6 py-3 rounded-[1.2rem] border border-zinc-200 text-[10px] font-black uppercase tracking-[0.3em] italic">Cancel</button><button onClick={performAction} disabled={!matchesConfirm} className={`px-6 py-3 rounded-[1.2rem] text-white text-[10px] font-black uppercase tracking-[0.3em] italic disabled:opacity-40 ${confirmAction.type === 'pause' ? 'bg-amber-500' : 'bg-rose-500'}`}>Confirm</button></div></motion.div></div>)}</AnimatePresence>
    </div>
  );
};

export default AdminPromoTasks;
