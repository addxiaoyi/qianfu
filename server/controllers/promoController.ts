import { Response, NextFunction } from 'express';
import crypto from 'crypto';
import prisma from '../db';
import { AuthRequest } from '../middleware/auth';
import { AppError, ErrorCode } from '../utils/errors';
import { sendCreatedResponse, sendDetailResponse, sendSuccess } from '../utils/response';

const isAdmin = (req: AuthRequest): boolean => req.isAdmin === true || req.user?.role === 'ADMIN';
const normalizePlatform = (platform: string): string => platform.trim().toLowerCase();
const makeRequestNo = (): string => `claim_${Date.now().toString(36)}_${crypto.randomBytes(6).toString('hex')}`;
const parseRuleConfig = (ruleConfig: unknown): Record<string, unknown> => { if (typeof ruleConfig === 'string') { try { return JSON.parse(ruleConfig) as Record<string, unknown>; } catch { throw new AppError('Invalid rule_config JSON', 400, ErrorCode.VALIDATION_ERROR); } } if (ruleConfig && typeof ruleConfig === 'object') return ruleConfig as Record<string, unknown>; throw new AppError('Missing rule_config', 400, ErrorCode.VALIDATION_ERROR); };
const getActiveBinding = async (userId: number, platform: string) => prisma.promoPlatformBinding.findFirst({ where: { user_id: userId, platform, binding_status: 'ACTIVE' } });
const verifyActionsLocally = (taskRuleConfig: Record<string, unknown>) => { const actions = (taskRuleConfig.actions as Record<string, unknown> | undefined) ?? {}; const condition = String(taskRuleConfig.condition ?? 'all_required'); const normalizedActions = { like: Boolean(actions.like), coin: Boolean(actions.coin), favorite: Boolean(actions.favorite), follow: Boolean(actions.follow), share: Boolean(actions.share) }; const enabledValues = Object.values(normalizedActions).filter(Boolean); const passed = condition === 'any_required' ? enabledValues.length > 0 : enabledValues.length > 0; return { passed, actions: normalizedActions, condition, reason: passed ? '' : 'Task rules require at least one enabled action' }; };
const validateTaskPayload = (body: Record<string, unknown>) => { const title = String(body.title ?? '').trim(); const platform = normalizePlatform(String(body.platform ?? '')); const targetId = String(body.targetId ?? '').trim(); const targetUrl = String(body.targetUrl ?? '').trim(); const rewardAmount = Math.trunc(Number(body.rewardAmount)); if (!title || !platform || !targetId || !targetUrl || !Number.isFinite(rewardAmount) || rewardAmount <= 0) throw new AppError('Missing or invalid required fields', 400, ErrorCode.VALIDATION_ERROR); return { title, platform, targetId, targetUrl, rewardAmount }; };

export const listPromoTasks = async (req: AuthRequest, res: Response, next: NextFunction) => { try { const tasks = await prisma.promoTask.findMany({ where: req.isAdmin ? {} : { status: 'ENABLED' }, orderBy: { created_at: 'desc' } }); const userId = req.user?.id; const platformBindings = userId ? await prisma.promoPlatformBinding.findMany({ where: { user_id: userId } }) : []; const taskIds = tasks.map((task) => task.id); const claims = taskIds.length > 0 ? await prisma.promoClaimRecord.findMany({ where: { task_id: { in: taskIds }, ...(userId ? { user_id: userId } : {}) } }) : []; const claimMap = new Map(claims.map((claim) => [claim.task_id, claim])); const bindingMap = new Map(platformBindings.map((binding) => [binding.platform, binding])); return sendSuccess(res, tasks.map((task) => ({ ...task, rule_config: task.rule_config, claimed: userId ? claimMap.has(task.id) : false, claim_status: claimMap.get(task.id)?.claim_status ?? null, reward_status: claimMap.get(task.id)?.reward_status ?? null, bound: userId ? bindingMap.has(normalizePlatform(task.platform)) : false })), 'Promo tasks loaded'); } catch (error) { next(error); } };

export const getPromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { const taskId = Number(req.params.id); const page = Math.max(1, Number(req.query.page ?? 1)); const limit = Math.min(50, Math.max(1, Number(req.query.limit ?? 10))); if (!Number.isInteger(taskId)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const task = await prisma.promoTask.findUnique({ where: { id: taskId } }); if (!task) throw new AppError('Task not found', 404, ErrorCode.NOT_FOUND); const [claims, totalClaims, auditLogs] = await Promise.all([ prisma.promoClaimRecord.findMany({ where: { task_id: task.id }, orderBy: { created_at: 'desc' }, skip: (page - 1) * limit, take: limit, include: { user: true } }), prisma.promoClaimRecord.count({ where: { task_id: task.id } }), prisma.promoVerifyLog.findMany({ where: { task_id: task.id }, orderBy: { created_at: 'desc' }, take: 20 }) ]); return sendDetailResponse(res, { ...task, claims, claimPagination: { page, limit, total: totalClaims, totalPages: Math.max(1, Math.ceil(totalClaims / limit)) }, auditLogs }, { resource: 'PromoTask' }); } catch (error) { next(error); } };

export const getPromoClaimDetail = async (req: AuthRequest, res: Response, next: NextFunction) => { try { const claimId = Number(req.params.id); if (!Number.isInteger(claimId)) throw new AppError('Invalid claim ID', 400, ErrorCode.VALIDATION_ERROR); const claim = await prisma.promoClaimRecord.findUnique({ where: { id: claimId }, include: { task: true } }); if (!claim) throw new AppError('Claim not found', 404, ErrorCode.NOT_FOUND); const walletTxs = await prisma.promoWalletTransaction.findMany({ where: { ref_type: 'promo_claim_record', ref_id: claim.id }, orderBy: { created_at: 'desc' }, take: 10 }); const verifyLogs = await prisma.promoVerifyLog.findMany({ where: { claim_id: claim.id }, orderBy: { created_at: 'desc' }, take: 10 }); return sendDetailResponse(res, { ...claim, walletTransactions: walletTxs, verifyLogs }, { resource: 'PromoClaimRecord' }); } catch (error) { next(error); } };

export const createPromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const { description, targetType, coverUrl, rewardType, ruleConfig, claimLimitPerUser, totalLimit, dailyLimit, needAudit, autoVerify, startAt, endAt, status } = req.body ?? {}; const validated = validateTaskPayload(req.body ?? {}); const parsedRuleConfig = parseRuleConfig(ruleConfig); const task = await prisma.promoTask.create({ data: { title: validated.title, description: description ? String(description) : null, platform: validated.platform, target_type: targetType ? String(targetType) : 'video', target_id: validated.targetId, target_url: validated.targetUrl, cover_url: coverUrl ? String(coverUrl) : null, reward_amount: validated.rewardAmount, reward_type: rewardType ? String(rewardType) : 'BALANCE', rule_config: JSON.stringify(parsedRuleConfig), claim_limit_per_user: claimLimitPerUser ? Math.trunc(Number(claimLimitPerUser)) : 1, total_limit: totalLimit !== undefined && totalLimit !== '' ? Math.trunc(Number(totalLimit)) : null, daily_limit: dailyLimit !== undefined && dailyLimit !== '' ? Math.trunc(Number(dailyLimit)) : null, need_audit: Boolean(needAudit), auto_verify: autoVerify !== undefined ? Boolean(autoVerify) : true, status: status ? String(status).toUpperCase() : 'DRAFT', start_at: startAt ? new Date(String(startAt)) : null, end_at: endAt ? new Date(String(endAt)) : null, created_by: req.user?.id ?? null } }); return sendCreatedResponse(res, task, { resource: 'PromoTask' }); } catch (error) { next(error); } };

export const updatePromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const taskId = Number(req.params.id); if (!Number.isInteger(taskId)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const task = await prisma.promoTask.findUnique({ where: { id: taskId } }); if (!task) throw new AppError('Task not found', 404, ErrorCode.NOT_FOUND); const { description, targetType, coverUrl, rewardType, ruleConfig, claimLimitPerUser, totalLimit, dailyLimit, needAudit, autoVerify, startAt, endAt, status } = req.body ?? {}; const validated = validateTaskPayload({ ...task, ...req.body }); const parsedRuleConfig = parseRuleConfig(ruleConfig ?? task.rule_config); const updated = await prisma.promoTask.update({ where: { id: taskId }, data: { title: validated.title, description: description !== undefined ? (description ? String(description) : null) : task.description, platform: validated.platform, target_type: targetType ? String(targetType) : task.target_type, target_id: validated.targetId, target_url: validated.targetUrl, cover_url: coverUrl !== undefined ? (coverUrl ? String(coverUrl) : null) : task.cover_url, reward_amount: validated.rewardAmount, reward_type: rewardType ? String(rewardType) : task.reward_type, rule_config: JSON.stringify(parsedRuleConfig), claim_limit_per_user: claimLimitPerUser ? Math.trunc(Number(claimLimitPerUser)) : task.claim_limit_per_user, total_limit: totalLimit !== undefined && totalLimit !== '' ? Math.trunc(Number(totalLimit)) : task.total_limit, daily_limit: dailyLimit !== undefined && dailyLimit !== '' ? Math.trunc(Number(dailyLimit)) : task.daily_limit, need_audit: needAudit !== undefined ? Boolean(needAudit) : task.need_audit, auto_verify: autoVerify !== undefined ? Boolean(autoVerify) : task.auto_verify, status: status ? String(status).toUpperCase() : task.status, start_at: startAt !== undefined ? (startAt ? new Date(String(startAt)) : null) : task.start_at, end_at: endAt !== undefined ? (endAt ? new Date(String(endAt)) : null) : task.end_at } }); return sendSuccess(res, updated, 'Task updated'); } catch (error) { next(error); } };

export const publishPromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const taskId = Number(req.params.id); if (!Number.isInteger(taskId)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const task = await prisma.promoTask.findUnique({ where: { id: taskId } }); if (!task) throw new AppError('Task not found', 404, ErrorCode.NOT_FOUND); const ruleConfig = parseRuleConfig(task.rule_config); const validation = verifyActionsLocally(ruleConfig); if (!validation.passed) throw new AppError(validation.reason, 400, ErrorCode.VALIDATION_ERROR); const updated = await prisma.promoTask.update({ where: { id: taskId }, data: { status: 'ENABLED', published_by: req.user?.id ?? null, published_at: new Date() } }); return sendSuccess(res, updated, 'Task published'); } catch (error) { next(error); } };

export const pausePromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const taskId = Number(req.params.id); if (!Number.isInteger(taskId)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const updated = await prisma.promoTask.update({ where: { id: taskId }, data: { status: 'PAUSED' } }); return sendSuccess(res, updated, 'Task paused'); } catch (error) { next(error); } };

export const disablePromoTask = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const taskId = Number(req.params.id); if (!Number.isInteger(taskId)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const updated = await prisma.promoTask.update({ where: { id: taskId }, data: { status: 'DISABLED' } }); return sendSuccess(res, updated, 'Task disabled'); } catch (error) { next(error); } };

export const bindPlatformAccount = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!req.user) throw new AppError('Unauthorized', 401, ErrorCode.UNAUTHORIZED); const { platform, platformUserId, platformUsername } = req.body ?? {}; if (!platform || !platformUserId) throw new AppError('Missing required fields', 400, ErrorCode.VALIDATION_ERROR); const userId = req.user.id; const normalizedPlatform = normalizePlatform(platform); const binding = await prisma.promoPlatformBinding.upsert({ where: { platform_platform_user_id: { platform: normalizedPlatform, platform_user_id: String(platformUserId) } }, update: { user_id: userId, platform_username: platformUsername || null, binding_status: 'ACTIVE', bind_source: 'MANUAL', verified_at: new Date(), last_verify_at: new Date() }, create: { user_id: userId, platform: normalizedPlatform, platform_user_id: String(platformUserId), platform_username: platformUsername || null, binding_status: 'ACTIVE', bind_source: 'MANUAL', verified_at: new Date(), last_verify_at: new Date() } }); return sendSuccess(res, binding, 'Platform account bound'); } catch (error) { next(error); } };

export const getMyBindings = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!req.user) throw new AppError('Unauthorized', 401, ErrorCode.UNAUTHORIZED); const userId = req.user.id; const bindings = await prisma.promoPlatformBinding.findMany({ where: { user_id: userId } }); return sendSuccess(res, bindings, 'Bindings loaded'); } catch (error) { next(error); } };

export const submitPromoClaim = async (req: AuthRequest, res: Response, next: NextFunction) => { try { const user = req.user; if (!user) throw new AppError('Unauthorized', 401, ErrorCode.UNAUTHORIZED); const userId = user.id; const { taskId, proofData } = req.body ?? {}; if (!taskId) throw new AppError('Missing taskId', 400, ErrorCode.VALIDATION_ERROR); const id = Number(taskId); if (!Number.isInteger(id)) throw new AppError('Invalid task ID', 400, ErrorCode.VALIDATION_ERROR); const task = await prisma.promoTask.findUnique({ where: { id } }); if (!task) throw new AppError('Task not found', 404, ErrorCode.NOT_FOUND); if (task.status !== 'ENABLED') throw new AppError('Task is not enabled', 400, ErrorCode.VALIDATION_ERROR); if (task.start_at && task.start_at > new Date()) throw new AppError('Task has not started yet', 400, ErrorCode.VALIDATION_ERROR); if (task.end_at && task.end_at < new Date()) throw new AppError('Task has expired', 400, ErrorCode.VALIDATION_ERROR); const binding = await getActiveBinding(userId, normalizePlatform(task.platform)); if (!binding) throw new AppError('Platform account not bound', 400, ErrorCode.VALIDATION_ERROR); const existingClaim = await prisma.promoClaimRecord.findFirst({ where: { user_id: userId, task_id: task.id }, orderBy: { created_at: 'desc' } }); if (existingClaim) return sendSuccess(res, existingClaim, 'Task already claimed'); const ruleConfig = parseRuleConfig(task.rule_config); const verifyResult = task.auto_verify ? verifyActionsLocally(ruleConfig) : { passed: !task.need_audit, actions: {}, condition: 'manual', reason: task.need_audit ? 'Awaiting audit' : '' }; const claimRequestNo = makeRequestNo(); const claim = await prisma.$transaction(async (tx) => { const createdClaim = await tx.promoClaimRecord.create({ data: { task_id: task.id, user_id: userId, platform_user_id: binding.platform_user_id, claim_status: verifyResult.passed && !task.need_audit ? 'VERIFIED' : task.need_audit ? 'PENDING' : 'REJECTED', reward_status: verifyResult.passed && !task.need_audit ? 'REWARDING' : 'PENDING', verify_result: JSON.stringify(verifyResult), verify_detail: JSON.stringify({ proofData: proofData ?? null, ruleConfig }), proof_data: proofData ? JSON.stringify(proofData) : null, claim_request_no: claimRequestNo, claim_at: new Date(), verified_at: verifyResult.passed && !task.need_audit ? new Date() : null, rewarding_at: verifyResult.passed && !task.need_audit ? new Date() : null, failed_reason: verifyResult.passed ? null : verifyResult.reason } }); await tx.promoVerifyLog.create({ data: { claim_id: createdClaim.id, task_id: task.id, user_id: userId, platform_user_id: binding.platform_user_id, verify_status: verifyResult.passed ? 'PASSED' : 'FAILED', request_data: JSON.stringify({ proofData: proofData ?? null }), response_data: JSON.stringify(verifyResult), error_message: verifyResult.passed ? null : verifyResult.reason, source: task.auto_verify ? 'AUTO' : 'MANUAL' } }); if (!verifyResult.passed || task.need_audit) return createdClaim; const wallet = await tx.wallet.upsert({ where: { user_id: userId }, update: {}, create: { user_id: userId, balance: 0, currency: 'CNY', is_active: true } }); const beforeBalance = wallet.balance; const afterBalance = beforeBalance + task.reward_amount; await tx.wallet.update({ where: { user_id: userId }, data: { balance: { increment: task.reward_amount } } }); await tx.promoWalletTransaction.create({ data: { user_id: userId, change_amount: task.reward_amount, direction: 'INCOME', change_type: 'PROMO_REWARD', ref_type: 'promo_claim_record', ref_id: createdClaim.id, before_balance: beforeBalance, after_balance: afterBalance, remark: `Promo reward for task ${task.id}`, created_by: userId } }); await tx.promoClaimRecord.update({ where: { id: createdClaim.id }, data: { claim_status: 'REWARDED', reward_status: 'REWARDED', rewarded_at: new Date() } }); return createdClaim; }); return sendCreatedResponse(res, claim, { resource: 'PromoClaimRecord' }); } catch (error) { next(error); } };

export const getMyPromoClaims = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!req.user) throw new AppError('Unauthorized', 401, ErrorCode.UNAUTHORIZED); const userId = req.user.id; const claims = await prisma.promoClaimRecord.findMany({ where: { user_id: userId }, orderBy: { created_at: 'desc' }, include: { task: true } }); return sendSuccess(res, claims, 'Promo claims loaded'); } catch (error) { next(error); } };

export const approvePromoClaim = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const claimId = Number(req.params.id); if (!Number.isInteger(claimId)) throw new AppError('Invalid claim ID', 400, ErrorCode.VALIDATION_ERROR); const claim = await prisma.promoClaimRecord.findUnique({ where: { id: claimId }, include: { task: true } }); if (!claim) throw new AppError('Claim not found', 404, ErrorCode.NOT_FOUND); if (claim.reward_status === 'REWARDED') return sendSuccess(res, claim, 'Claim already rewarded'); const updated = await prisma.$transaction(async (tx) => { const wallet = await tx.wallet.upsert({ where: { user_id: claim.user_id }, update: {}, create: { user_id: claim.user_id, balance: 0, currency: 'CNY', is_active: true } }); const beforeBalance = wallet.balance; const afterBalance = beforeBalance + claim.task.reward_amount; await tx.wallet.update({ where: { user_id: claim.user_id }, data: { balance: { increment: claim.task.reward_amount } } }); await tx.promoWalletTransaction.create({ data: { user_id: claim.user_id, change_amount: claim.task.reward_amount, direction: 'INCOME', change_type: 'PROMO_REWARD', ref_type: 'promo_claim_record', ref_id: claim.id, before_balance: beforeBalance, after_balance: afterBalance, remark: `Manual approve for task ${claim.task_id}`, created_by: req.user?.id ?? null } }); return tx.promoClaimRecord.update({ where: { id: claim.id }, data: { claim_status: 'REWARDED', reward_status: 'REWARDED', verified_at: claim.verified_at ?? new Date(), rewarding_at: new Date(), rewarded_at: new Date(), audit_by: req.user?.id ?? null, audit_note: req.body?.remark || 'Approved', failed_reason: null } }); }); return sendSuccess(res, updated, 'Claim approved and rewarded'); } catch (error) { next(error); } };

export const rejectPromoClaim = async (req: AuthRequest, res: Response, next: NextFunction) => { try { if (!isAdmin(req)) throw new AppError('Forbidden', 403, ErrorCode.FORBIDDEN); const claimId = Number(req.params.id); if (!Number.isInteger(claimId)) throw new AppError('Invalid claim ID', 400, ErrorCode.VALIDATION_ERROR); const updated = await prisma.promoClaimRecord.update({ where: { id: claimId }, data: { claim_status: 'REJECTED', reward_status: 'PENDING', audit_by: req.user?.id ?? null, audit_note: req.body?.remark || 'Rejected', failed_reason: req.body?.remark || 'Rejected by admin' } }); return sendSuccess(res, updated, 'Claim rejected'); } catch (error) { next(error); } };

export const getPromoAuditSummary = async (_req: AuthRequest, res: Response, next: NextFunction) => { try { const [tasks, claims, bindings] = await Promise.all([prisma.promoTask.count(), prisma.promoClaimRecord.count(), prisma.promoPlatformBinding.count()]); const rewarded = await prisma.promoClaimRecord.count({ where: { reward_status: 'REWARDED' } }); const pending = await prisma.promoClaimRecord.count({ where: { claim_status: 'PENDING' } }); const rejected = await prisma.promoClaimRecord.count({ where: { claim_status: 'REJECTED' } }); return sendSuccess(res, { tasks, claims, bindings, rewarded, pending, rejected }, 'Promo summary loaded'); } catch (error) { next(error); } };
